# Base URL of your Strapi API. The frontend fetches content from here at build time.
# Note: NEXT_PUBLIC_ prefix makes it available in the browser too (needed for image URLs).
NEXT_PUBLIC_STRAPI_URL=https://api.connectorselection.com
